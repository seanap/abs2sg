(()=>{const h=prompt("Paste full Cookie header value from Network request:");if(!h)throw new Error("No cookie header pasted");const cookies=h.split(";").map(s=>s.trim()).filter(Boolean).map(p=>{const i=p.indexOf("=");return{name:p.slice(0,i),value:p.slice(i+1),domain:"app.thestorygraph.com",path:"/",expires:-1,httpOnly:false,secure:true,sameSite:"Lax"};});const ls=[];for(let i=0;i<localStorage.length;i++){const k=localStorage.key(i);ls.push({name:k,value:localStorage.getItem(k)||""});}const state={cookies,origins:[{origin:"https://app.thestorygraph.com",localStorage:ls}]};const b64=btoa(unescape(encodeURIComponent(JSON.stringify(state))));copy(b64);console.log("Copied SG_STORAGE_STATE_B64. Cookies:",cookies.length);})();
